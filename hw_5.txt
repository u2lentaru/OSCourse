Ссылка -> https://www.youtube.com/watch?v=OTc9B_whZS0
.
Общие команды
fdisk -l - список всех устройств
fdisk /dev/sda - заходим в утилиту fdisk диска sda
Далее в интерфейсе fdisk n = создать новый раздел (+1G), d = удалить.

​
mkfs.ext4 /dev/sda1 - создать ФС на разделе
tune2fs -l /dev/sda1 - просмотр суперблока ФС
​
mkdir /mnt/hello- создем точку монтирования
mount /dev/sdc1 /mnt/hello - монтируем раздел
df -h - просмотр смонтированных устройств
​
Увеличение ФС
1. fdisk /dev/sda- удаляем раздел командой d и создаем новый с увеличенным размером (+3G)
2. Выходим с сохранением w и при необходимости даем понять системе, что раздел изменился - командой partrobe.
Можно самостоятельно проверить, поняла ли система, в каталоге cat /proc/partitions
3. resize2fs /dev/sda1 - переоперделяем размер ФС.
e2fsck -f /dev/sda1 - проверка диска (при необходимости)
Уменьшение ФС
1. unmount /mnt/hello- размонтируем раздел
2. resize2fs /dev/sda1 1G - меняем размер ФС в меньшую сторону
3. mount /dev/sdc1/ mnt/hello - монтируем раздел
4. fdisk /dev/sda- удаляем раздел командой d и создаем новый с уменьшенным размером (+2G вместо трех, например)
partrobe - при необходимости
5. resize2fs /dev/sda1 - расширяем ФС (если до этого задавали с заделом меньше)
.
Работа с LVM:
Хорошо описана здесь и здесь
https://habr.com/ru/post/67283/
http://xgu.ru/wiki/LVM#.D0.A1.D0.BE.D0.B7.D0.B4.D0.B0.D0.BD.D0.B8.D0.B5_.D0.B3.D1.80.D1.83.D0.BF.D0.BF.D1.8B_.D1.82.D0.BE.D0.BC.D0.BE.D0.B2


* - по желанию
1. Почему мы не можем уменьшить файловую систему без простоя?
2. Что есть LV в рамках LVM?
3. Монтирование файловой системы – …
a. Подключение файловой системы к уже подключенной иерархии файловой системы, используемой в операционной системе
b. Настройка файловой системы с помощью соответствующей утилиты, изменение её параметров.
c. Разметка файловой системы, создание секторов, дорожек и таблицы файловой системы
d. Подключение драйвера файловой системы для работы с ней
4. Для чего мы монтируем?
5*. Установите LVM, создате PV, VG, пару LV. После добавьте еще один логический том и расширьте на все доступное пространство 
один имеющийся LV. Результат предоставить по принципу прошлого урока :)

1. Требуется размонтировать и после изменения размера снова смонтировать раздел.
2. Logical Value, логический раздел.
3. a.
4. Чтобы сделать соответствующее дисковое устройство доступным для использования.

